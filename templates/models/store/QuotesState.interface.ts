import { {{ Name }}Interface } from '../{{ name }}.interface'

/**
 * @name {{ Name }}sStateInterface
 * @description
 * Interface for the {{ Name }}s state
 */
export interface {{ Name }}sStateInterface {
  loading: boolean
  {{ name }}s: {{ Name }}Interface[]
}
