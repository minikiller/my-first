import { {{ Name }}sStateInterface } from '../models/store'

/**
 * @name initial{{ Name }}sState
 * @description
 * The {{ Name }}s state instance with the initial default values
 */
export const initial{{ Name }}sState: {{ Name }}sStateInterface = {
  loading: false,
  {{ name }}s: []
}
